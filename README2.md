```md
# Drop (Delete) an Existing Database in SQL

## Description
This SQL query permanently deletes an existing database called `demo`.

## SQL Query
```sql
DROP DATABASE demo;
```

## Usage
1. Open your SQL database management tool (e.g., MySQL, PostgreSQL).
2. Run the query in the SQL command interface.
3. The `demo` database will be permanently deleted.
