```md
# Create a New Database in SQL

## Description
This SQL query creates a new database called `salesDB`.

## SQL Query
```sql
CREATE DATABASE salesDB;
```

## Usage
1. Open your SQL database management tool (e.g., MySQL, PostgreSQL).
2. Run the query in the SQL command interface.
3. The `salesDB` database will be created and ready for use.
```